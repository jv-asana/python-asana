: 
    get_favorites_for_user: >-
        import asana


        client = asana.Client.access_token('PERSONAL_ACCESS_TOKEN')


        result = client.users.get_favorites_for_user(user_gid, {'field': 'value', 'field': 'value'}, opt_pretty=True)
    get_user: >-
        import asana


        client = asana.Client.access_token('PERSONAL_ACCESS_TOKEN')


        result = client.users.get_user(user_gid, {'field': 'value', 'field': 'value'}, opt_pretty=True)
    get_users: >-
        import asana


        client = asana.Client.access_token('PERSONAL_ACCESS_TOKEN')


        result = client.users.get_users({'field': 'value', 'field': 'value'}, opt_pretty=True)
    get_users_for_team: >-
        import asana


        client = asana.Client.access_token('PERSONAL_ACCESS_TOKEN')


        result = client.users.get_users_for_team(team_gid, {'field': 'value', 'field': 'value'}, opt_pretty=True)
    get_users_for_workspace: >-
        import asana


        client = asana.Client.access_token('PERSONAL_ACCESS_TOKEN')


        result = client.users.get_users_for_workspace(workspace_gid, {'field': 'value', 'field': 'value'}, opt_pretty=True)
