: 
    get_team_membership: >-
        import asana


        client = asana.Client.access_token('PERSONAL_ACCESS_TOKEN')


        result = client.team memberships.get_team_membership(team_membership_gid, {'field': 'value', 'field': 'value'}, opt_pretty=True)
    get_team_memberships: >-
        import asana


        client = asana.Client.access_token('PERSONAL_ACCESS_TOKEN')


        result = client.team memberships.get_team_memberships({'field': 'value', 'field': 'value'}, opt_pretty=True)
    get_team_memberships_for_team: >-
        import asana


        client = asana.Client.access_token('PERSONAL_ACCESS_TOKEN')


        result = client.team memberships.get_team_memberships_for_team(team_gid, {'field': 'value', 'field': 'value'}, opt_pretty=True)
    get_team_memberships_for_user: >-
        import asana


        client = asana.Client.access_token('PERSONAL_ACCESS_TOKEN')


        result = client.team memberships.get_team_memberships_for_user(user_gid, {'field': 'value', 'field': 'value'}, opt_pretty=True)
