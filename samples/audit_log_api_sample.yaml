: 
    get_audit_log_events: >-
        import asana


        client = asana.Client.access_token('PERSONAL_ACCESS_TOKEN')

        result = client.audit log api.get_audit_log_events(workspace_gid, {'param': 'value', 'param': 'value'}, opt_pretty=True)
