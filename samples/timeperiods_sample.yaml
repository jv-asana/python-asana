: 
    get_time_period: >-
        import asana


        client = asana.Client.access_token('PERSONAL_ACCESS_TOKEN')


        result = client.time periods.get_time_period(time_period_gid, {'field': 'value', 'field': 'value'}, opt_pretty=True)
    get_time_periods: >-
        import asana


        client = asana.Client.access_token('PERSONAL_ACCESS_TOKEN')


        result = client.time periods.get_time_periods({'field': 'value', 'field': 'value'}, opt_pretty=True)
